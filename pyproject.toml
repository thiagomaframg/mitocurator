[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mitocurator"
version = "0.1.0-dev"
requires-python = ">=3.11"
dependencies = ["biopython", "pandas", "pyyaml"]

[project.scripts]
mitocurator = "mitocurator.cli:main"
